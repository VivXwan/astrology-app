# .env.example
# Database connection string (PostgreSQL format)
DATABASE_URL=postgresql://username:password@host:port/database_name

# JWT secret key (should be a strong, unique value)
SECRET_KEY=your-secure-secret-key-here
